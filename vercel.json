{
  "version": 2,
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "dist/app-angular",
  "installCommand": "npm install",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/tmdb.ts" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}